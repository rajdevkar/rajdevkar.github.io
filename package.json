{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://rajdevkar.github.io",
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start",
    "deploy": "next build && next export && git add out/ && git commit -m \"Deploy gh-pages\" && git subtree push --prefix out origin gh-pages",
    "postbuild": "cp -a out/. ./build"
  },
  "dependencies": {
    "next": "10.0.4",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "@tailwindcss/jit": "^0.1.18",
    "@types/node": "^7.0.6",
    "@types/react": "^16.4.16",
    "autoprefixer": "^10.3.1",
    "postcss": "^8.3.6",
    "postcss-preset-env": "^6.7.0",
    "tailwindcss": "^2.2.7",
    "typescript": "^4.1.3"
  }
}
